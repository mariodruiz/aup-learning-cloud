name: Lint

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  python-lint:
    name: Python (Ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Ruff
        run: pip install ruff

      - name: Run Ruff linter
        run: ruff check .

      - name: Run Ruff formatter check
        run: ruff format --check .

  python-tests:
    name: Python Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install test deps
        run: pip install pytest pyyaml "pydantic>=2.0"

      - name: Run Python unit tests
        run: >-
          python -m pytest tests/installer -v
          runtime/hub/tests/test_spawn_defaults.py

  hub-regression-tests:
    name: Hub Regression Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install test deps
        run: pip install pytest anyio pyyaml traitlets "pydantic>=2.0"

      - name: Run Hub regression tests
        run: |
          python -m pytest -v \
            runtime/hub/tests/test_jupyterhub_config_startup.py \
            runtime/hub/tests/test_spawner_gpu_access.py \
            runtime/hub/tests/test_spawner_runtime_metadata.py \
            runtime/hub/tests/test_authenticator_factory.py \
            runtime/hub/tests/test_auth_provider_setup.py \
            runtime/hub/tests/test_github_authenticator.py \
            runtime/hub/tests/test_native_authenticator.py

  frontend-lint:
    name: Frontend (ESLint + TypeScript)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: runtime
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: runtime/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install

      - name: Run ESLint
        run: pnpm run lint

      - name: Build (TypeScript check)
        run: pnpm run build

  shell-lint:
    name: Shell (ShellCheck)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck
        run: sudo apt-get install -y shellcheck

      - name: Run ShellCheck
        run: |
          find . -name "*.sh" -o -name "*.bash" | \
            grep -v node_modules | \
            grep -v .git | \
            xargs -r shellcheck

      - name: Run shell tests
        run: |
          dockerfiles/Code/tests/test_runtime_extension_model.sh
          dockerfiles/Code/tests/test_service_lifecycle.sh

  yaml-lint:
    name: YAML (yamllint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install yamllint
        run: pip install yamllint

      - name: Run yamllint
        run: yamllint .
